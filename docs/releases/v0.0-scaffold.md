# `v0.0-scaffold` — repo skeleton

**Date:** 2026-05-02

## Scope

Bring up the empty repo with the minimum viable structure so every later milestone has somewhere to land. No ML, no data, no pipeline.

## Delivered

- `pyproject.toml` with `uv`, `ruff`, `pytest` (markers: `eval` for slow tests).
- `src/sarcolit/` package with empty subpackages (`ingest`, `retrieval`, `generation`, `eval`, `agent`, `serve`) — each subpackage's docstring states the milestone it lands in.
- `.github/workflows/ci.yml` running ruff + pytest (cheap subset) on every push/PR.
- MIT license, `.gitignore`, `.env.example`.
- `docs/architecture.md` (target component map + dated decisions log), `docs/weekly_log.md` (engineering log starts here), `docs/releases/` (per-tag notes).
- `README.md` with the milestone roadmap, eval-results table (empty), and project layout.

## Eval delta vs previous tag

n/a — first tag.

## Known issues / debt

- No tests yet — CI currently just lints. First real test arrives with `v0.1-corpus` (ingest determinism check).
- `data/` is gitignored but has no README explaining what should land there. Will be addressed in `v0.1-corpus`.
- Repo not yet pushed to GitHub — pending Chawki creating the public remote.

## Next milestone

`v0.1-corpus` — PubMed E-utilities ingestion of sarcopenia / EMG abstracts, deduplication, chunking, metadata, versioned dataset on disk (HF datasets format).