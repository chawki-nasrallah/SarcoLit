# Copy to .env (gitignored) and fill in as milestones add API integrations.
# v0.1+: PubMed E-utilities (no key required for low rates, but recommended)
NCBI_EMAIL=
NCBI_API_KEY=

# v0.2+: generation backend
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# v0.7+: HuggingFace (model + Spaces)
HF_TOKEN=